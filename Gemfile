# RVM can read Gemfiles to know what Ruby (-ies) to load upon entering the
# project directory. The first line must stay a comment; the second line can be
# uncommented if you have a version of bundler capable of dealing with ruby
# directives as well as gem directives
#ruby=ruby-2.3.0

ruby '2.3.0'

# Required to parse the info.toml files
gem 'tomlrb', '~> 1.2'

# Optional to use bin/console with a prompt I consider superior to IRb
#gem 'pry', '~> 0.10'
